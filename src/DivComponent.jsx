import React from 'react';

/**
 * DivComponent is a functional React component that returns the same content as TextComponent,
 * but wrapped in a HTML div element.
 * 
 * @type {React.FunctionComponent}
 */
const DivComponent = () => {
  return <div>Text</div>;
};

export default DivComponent;
