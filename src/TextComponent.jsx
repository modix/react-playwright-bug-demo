/**
 * TextComponent is a functional React component that renders a plain text string.
 * In React, strings are valid renderable nodes and will appear as text in the DOM.
 * 
 * @type {React.FunctionComponent}
 */
const TextComponent = () => {
    return 'Text';
};

export default TextComponent;
